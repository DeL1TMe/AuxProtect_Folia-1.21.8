# 🚀 Полное руководство по установке и сборке AuxProtect для Folia

## TL;DR (Для нетерпеливых)

**Windows:**
```powershell
powershell -ExecutionPolicy Bypass -File "advanced-install.ps1"
# После выполнения (может потребоваться перезагрузка системы):
mvn clean package -DskipTests
```

---

## 📋 Содержание

1. [Требования](#требования)
2. [Быстрая установка](#быстрая-установка)
3. [Ручная установка](#ручная-установка)
4. [Решение проблем](#решение-проблем)
5. [Сборка проекта](#сборка-проекта)

---

## Требования

- **Windows 10/11** или **macOS** или **Linux**
- **Java 16+** (рекомендуется JDK 21)
- **Maven 3.6+** (рекомендуется 3.9+)
- **Интернет соединение** для скачивания зависимостей

---

## Быстрая установка

### Windows

#### Вариант 1: Автоматическая установка (Рекомендуется)

```powershell
# Откройте PowerShell как администратор и выполните:
cd C:\Users\Denis\Downloads\AuxProtect-main
powershell -ExecutionPolicy Bypass -File "advanced-install.ps1"
```

Скрипт:
- ✅ Проверит winget
- ✅ Установит Java 21
- ✅ Установит Maven
- ✅ Добавит в PATH
- ✅ Проверит установку

#### Вариант 2: Ручная установка через winget

```powershell
winget install --id Oracle.OpenJDK.21 -e
winget install --id Apache.Maven -e
```

Затем перезагрузите систему.

#### Вариант 3: Ручная загрузка и установка

Смотрите [MANUAL_INSTALL_JAVA_MAVEN.md](MANUAL_INSTALL_JAVA_MAVEN.md)

---

### macOS

#### С Homebrew (Рекомендуется)

```bash
# Установить Homebrew если еще нет
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить Java и Maven
brew install openjdk@21 maven
```

#### Без Homebrew

1. Скачать Java с https://adoptium.net/
2. Скачать Maven с https://maven.apache.org/download.cgi
3. Распаковать в `/opt/`
4. Добавить в `~/.zshrc` или `~/.bash_profile`:
```bash
export JAVA_HOME="/opt/jdk-21"
export MAVEN_HOME="/opt/apache-maven-3.9.5"
export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"
```

---

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install openjdk-21-jdk maven
```

### Linux (Fedora/RHEL)

```bash
sudo dnf install java-21-openjdk maven
```

---

## Ручная установка

Если автоматическая установка не сработала, смотрите [MANUAL_INSTALL_JAVA_MAVEN.md](MANUAL_INSTALL_JAVA_MAVEN.md)

---

## Решение проблем

### Проблема: "mvn: command not found"

**Решение:**
1. Перезагрузите систему полностью
2. Откройте новый терминал/PowerShell
3. Проверьте: `java -version` и `mvn -version`

### Проблема: PowerShell script execution is disabled

**Решение:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope CurrentUser
powershell -ExecutionPolicy Bypass -File "advanced-install.ps1"
```

### Проблема: "No such file or directory" при распаковке

**Решение:**
- Убедитесь что распаковка произошла в правильную папку
- Проверьте что путь не содержит специальные символы

### Проблема: Out of memory при сборке

**Решение:**
```bash
export MAVEN_OPTS="-Xmx2g"
mvn clean package -DskipTests
```

### Проблема: Maven загружает зависимости очень долго

**Решение:**
- Это нормально при первой сборке
- Дождитесь завершения (может занять 5-10 минут)
- При последующих сборках будет быстрее

---

## Сборка проекта

### Проверка установки

Убедитесь что Java и Maven установлены:

```bash
java -version
mvn -version
```

Должно вывести версии обоих инструментов.

### Сборка

```bash
cd C:\Users\Denis\Downloads\AuxProtect-main
mvn clean package -DskipTests
```

Или используя скрипт:

**Windows:**
```powershell
.\build.bat
```

**macOS/Linux:**
```bash
chmod +x build.sh
./build.sh
```

### Результат сборки

Если сборка успешна, вы увидите:

```
[INFO] BUILD SUCCESS
```

JAR файл будет находиться в:

```
AuxProtect_Core/target/AuxProtect-1.3.4-pre2.jar
```

---

## Что дальше

После успешной сборки:

1. **Копируем JAR на сервер:**
```bash
cp AuxProtect_Core/target/AuxProtect-1.3.4-pre2.jar server/plugins/
```

2. **Запускаем сервер:**
```bash
java -Xmx4G -jar folia-1.21.8.jar nogui
```

3. **Проверяем загрузку плагина:**
```
/plugins
```

---

## Информация о версиях

- **AuxProtect**: 1.3.4-pre2
- **Minecraft**: 1.21.8
- **Java**: 16+ (рекомендуется 21 LTS)
- **Maven**: 3.6+ (рекомендуется 3.9+)

---

## Получение помощи

Если у вас возникли проблемы:

1. Проверьте [Решение проблем](#решение-проблем) выше
2. Прочитайте [MANUAL_INSTALL_JAVA_MAVEN.md](MANUAL_INSTALL_JAVA_MAVEN.md)
3. Проверьте логи сборки: `mvn clean package -DskipTests | tee build.log`
4. Создайте issue на GitHub с логами

---

## Полезные ссылки

- [Java Adoptium](https://adoptium.net/) - Скачать OpenJDK
- [Maven Official](https://maven.apache.org/) - Скачать Maven
- [Folia](https://github.com/PaperMC/Folia) - Folia GitHub
- [AuxProtect Wiki](https://github.com/Heliosares/AuxProtect/wiki) - Документация плагина

---

## Статус

✅ Все инструменты установки готовы  
✅ Код адаптирован для Folia 1.21.8  
✅ Полная документация создана  

**Готово к сборке!** 🚀

---

**Дата**: 22 января 2026  
**Версия**: 1.3.4-pre2  
**Статус**: READY FOR BUILD
