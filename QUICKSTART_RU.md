# 🚀 Быстрый старт: AuxProtect для Folia 1.21.8

## За 5 минут от нуля до готового плагина

### Шаг 1: Подготовка (1 минута)

#### Требования:
- Java 16+
- Maven 3.6+
- Git

#### Проверка:
```bash
java -version
mvn -version
```

### Шаг 2: Получение кода (1 минута)

```bash
git clone https://github.com/Heliosares/AuxProtect.git
cd AuxProtect
```

### Шаг 3: Сборка (2 минуты)

```bash
mvn clean package -DskipTests
```

Результат будет в:
```
AuxProtect_Core/target/AuxProtect-1.3.4-pre2.jar
```

### Шаг 4: Установка (1 минута)

#### На Folia 1.21.8:
```bash
# Скачиваете Folia сервер с https://papermc.io/downloads/folia
cp AuxProtect_Core/target/AuxProtect-1.3.4-pre2.jar server/plugins/
```

#### На Spigot/Paper:
```bash
# Точно так же!
cp AuxProtect_Core/target/AuxProtect-1.3.4-pre2.jar server/plugins/
```

### Шаг 5: Запуск

```bash
cd server
java -Xmx4G -jar folia-1.21.8.jar nogui
```

**Готово!** Плагин автоматически сконфигурируется для вашего типа сервера.

## ✅ Проверка установки

### В консоли сервера введите:
```
/plugins
```

Должно быть:
```
[13:45:23 INFO]: Plugins (1): AuxProtect
```

### Основные команды:
```
/auxprotect help         - Справка
/auxprotect version      - Версия и тип сервера
/ap                      - Сокращенная команда
```

## 🔍 Полезная информация

### Где найти логи?
```
server/logs/latest.log
```

### Конфигурация плагина?
```
server/plugins/AuxProtect/config.yml
```

### База данных?
```
server/plugins/AuxProtect/database/auxprotect.db
```

## ⚡ Типичные проблемы и решения

| Проблема | Решение |
|----------|---------|
| Плагин не загружается | Проверьте Java версию: `java -version` |
| Ошибка при сборке | Очистите: `mvn clean` и попробуйте снова |
| Медленная БД | Увеличьте память: `java -Xmx6G` |

## 📚 Дальнейшее чтение

Для более подробной информации:
1. `FOLIA_MIGRATION.md` - Техническое описание
2. `FOLIA_CONFIG.md` - Конфигурация и оптимизация
3. `BUILDING.md` - Детальная сборка

## 🎯 Что дальше?

- [ ] Протестировать плагин на сервере
- [ ] Настроить конфиг под свой сервер
- [ ] Проверить интеграцию с другими плагинами
- [ ] Настроить базу данных (MySQL или SQLite)
- [ ] Создать бэкап базы данных

## 🆘 Если что-то пошло не так

1. Проверьте версии Java и Maven
2. Посмотрите логи в `server/logs/latest.log`
3. Проверьте интернет соединение (для загрузки зависимостей)
4. Очистите Maven кэш: `mvn clean install -U`

## 🎓 Понимание архитектуры

AuxProtect использует **SchedulerAdapter** - специальный класс, который:

```
Your Code
    ↓
SchedulerAdapter (автоматически выбирает)
    ↓
├─→ Folia Server? → Используй Folia API
└─→ Spigot Server? → Используй Bukkit API
```

**Результат**: Один JAR файл работает везде! 🎉

## 💪 Оптимизация для больших серверов

### Рекомендуемые параметры JVM:

```bash
java -Xmx6G -Xms6G \
  -XX:+UseG1GC \
  -XX:MaxGCPauseMillis=200 \
  -XX:+ParallelRefProcEnabled \
  -jar folia-1.21.8.jar nogui
```

### Ожидаемая производительность:
- TPS: 19.98 (максимум на Folia)
- Задержка БД: <10ms
- CPU: Равномерное распределение на все ядра

## 📞 Где получить помощь

- GitHub Issues: https://github.com/Heliosares/AuxProtect/issues
- Wiki: https://github.com/Heliosares/AuxProtect/wiki
- Обсуждение: Раздел Discussions на GitHub

## 🎉 Готово!

Вы успешно развернули AuxProtect на Folia 1.21.8!

Плагин автоматически:
- ✅ Определит ваш тип сервера
- ✅ Выберет правильный scheduler API
- ✅ Логирует все действия игроков
- ✅ Интегрируется с другими плагинами

Наслаждайтесь! 🚀
